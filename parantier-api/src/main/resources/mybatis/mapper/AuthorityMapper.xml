<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.authority.domain.AuthorityRepository">

    <!-- 역할로 권한 이름 목록 조회 -->
    <select id="findAuthorityNamesByRole" resultType="string">
        SELECT a.name
        FROM authority a
        INNER JOIN role_authority ra ON a.id = ra.authority_id
        WHERE ra.role = #{role}
        ORDER BY a.category, a.name
    </select>

    <!-- 모든 권한 조회 -->
    <select id="findAll" resultType="com.mapo.palantier.authority.domain.Authority">
        SELECT
            id,
            name,
            description,
            category,
            created_at as createdAt
        FROM authority
        ORDER BY category, name
    </select>

    <!-- 카테고리별 권한 조회 -->
    <select id="findByCategory" resultType="com.mapo.palantier.authority.domain.Authority">
        SELECT
            id,
            name,
            description,
            category,
            created_at as createdAt
        FROM authority
        WHERE category = #{category}
        ORDER BY name
    </select>

</mapper>
